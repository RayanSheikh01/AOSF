"""Shared pytest fixtures for the AgentOS test suite.

Fixtures are added here as subsystems land (e.g. a fresh ``Kernel`` and a
``MockAgent`` factory). Kept intentionally empty at scaffold time so the suite
collects cleanly before any production code exists.
"""
